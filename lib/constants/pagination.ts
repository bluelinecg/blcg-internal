/** Default number of records per page across all list views. */
export const DEFAULT_PAGE_SIZE = 25;

/** Maximum pageSize accepted from the client to prevent oversized queries. */
export const MAX_PAGE_SIZE = 100;
